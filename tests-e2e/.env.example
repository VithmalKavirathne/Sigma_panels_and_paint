# Copy this file to ".env" and fill in your LOCAL dev values.
# .env is gitignored so credentials never enter version control.
BASE_URL=http://localhost:8088
ADMIN_EMAIL=admin@sigmapanels.com.au
# Set your local admin password here (do not commit .env):
ADMIN_PASSWORD=
